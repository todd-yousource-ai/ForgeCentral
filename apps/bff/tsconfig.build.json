{
  "//": "Build config: emit dist/ from src only. @forge/contracts resolves from its built dist (node_modules) here -- not the source paths override -- so rootDir stays inside src. pnpm -r builds contracts first (topological order).",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "rootDir": "src",
    "outDir": "dist",
    "paths": {}
  },
  "include": ["src"]
}
