# IP-CONSOLE-00-DEPLOY -- landing ledger

Per-PR landing record for `IP-CONSOLE-00-DEPLOY.md` (the Console is installer-provisioned; no live-proof
stitching remains manual). One PR per roster row, a named slice of `INV-CONSOLE-DEPLOY-REPRODUCIBLE`, the
full `scripts/ci.sh` green, branch-per-PR off local `main`, no-ff merge, push to `origin`, docs separate
from code. Reviewed with the maintainer before each merge.

Status: **D.1 + D.2 LANDED. D.3 DECIDED = ZTP + full-TPM + enrolled-role grant; the crdb ENGINE + ENROLLMENT sides of
D.3 are COMPLETE -- D.3b-1 (mechanism, crdb `b988d3f7`) + D.3b-2 (live `AigRoleResolver` + `CDB_WIRE_ROLE_GRANTS`,
crdb `4f861db2`) + D.3a crdb half (the sink writes `attributes["role"]` from a `role:` capability, crdb
`f8f9321e`), INV-WIRE-ENROLLED-ROLE-GRANT: the whole chain (IdP group -> `role:console-operator` capability
-> agent-record role -> wire admission -> least-privilege `[Data, Delegation]`) is in the engine repos,
config-authorable. Remaining is CONSOLE-side: D.3a-console (ForgeCentral drives `torch-enroll` presenting
the `role:console-operator` capability) + D.3c (MFA capstone). D.4 (end-to-end installer + productionized
CS.N self-check) depends on D.1-D.3.**

| Step | Invariant | Status | Commit | Proof |
|------|-----------|--------|--------|-------|
| D.1 | INV-CONSOLE-DEPLOY-REPRODUCIBLE (BFF process) | LANDED (review) | (this PR) | `apps/bff/deploy/`: `console-bff.service` (hardened systemd unit -- dedicated non-root `console-bff` user, `EnvironmentFile=/etc/console-bff/config.env`, `ExecStart` the platform node on the built BFF, `After=console-crypto-sidecar.service`, no caps, `ProtectSystem=strict`, `ReadOnlyPaths`, private tmp) + `config.example.env` (the `FC_*` env: `FC_HTTP_PORT` = the sidecar `admin_upstream`, `FC_ENGINE_HOST/PORT` = the sidecar egress loopback, the optional OIDC+RBAC block, `FC_SESSION_COOKIE_SECURE=true`) + a deploy README with the two-process topology + install steps. Turns the CS.N "ran node with env by hand" step into an installer-provisioned unit. Mirrors the sidecar's CS.5 deploy dir. `systemd-analyze verify` clean (bar the node/app paths that only exist post-install). Full `scripts/ci.sh --skip-sidecar` green. |
| D.2 | INV-CONSOLE-DEPLOY-REPRODUCIBLE (sidecar config + admin cert) | LANDED (review) | (this PR) | `sidecar/deploy/provision-sidecar.sh`: idempotent, env-configured. Mints the admin-plane ECDSA P-384 leaf (SAN `IP:$NODE_IP` + admin DNS; skipped when present unless `FORCE=1`, so an org-CA-signed CNSA cert can be pre-placed) and writes `config.json` from the node IP + loopback ports + the engine cert paths (the engine identity itself is D.3/ZTP; only referenced here). Chowns to the service user under root; prints the unit-install follow-up. **Proven:** the script ran, produced a valid P-384 leaf (SAN `IP:10.128.0.9`,`DNS:console-admin.localhost`) + config; re-run kept the cert (idempotent); the RELEASE sidecar binary loaded the generated config and bound both legs (`10.128.0.9:8443` + `127.0.0.1:8789`). Replaces the CS.N hand-run `openssl` + hand-written config. |
| D.3 | INV-CONSOLE-DEPLOY-REPRODUCIBLE (engine identity + grant) | DECIDED (ZTP + full-TPM); sub-plan open | -- | **Decisions (product owner 2026-07-10): ZTP enrollment, with FULL TPM ATTESTATION (torch-style).** The console-bff enrolls with a real vTPM device key + operator MFA + attestation, exactly like a torch edge device; engine admits it on `:7878` via `admit_wire_peer`. **D.3a** = a Console enroll step that REUSES `torch-enroll` (as the torch installer's `30-enroll.sh` does) with a distinct `console-bff` FQDN -> the wire cert the sidecar presents; live enrollment is operator-MFA-gated (human step). **D.3b (crdb, INV-CROSS, the crux)** = a per-identity/role enrolled-grant override so the console-bff resolves to `[Data, Delegation]` while every other enrolled device keeps the global `wire.enrolled_device_grant` default (`[Data,Agent,Cognition,Otlp]`); least-privilege, min-change, reviewed with the crdb maintainer; buildable + hermetically testable without MFA. **D.3c** = installer runs enrollment + sidecar uses the enrolled cert + CS.N engine leg passes with the ZTP identity + rotation (MFA-gated live capstone). See IP Section 4. **D.3b-1 LANDED** (crdb main `b988d3f7`, `INV-WIRE-ENROLLED-ROLE-GRANT`): `WireConfig.enrolled_role_grants` + `admit_wire_peer` applies a server-derived role's plane set on the enrolled branch only, fail-closed (absent/unmapped role -> the default that never carries Delegation), static peers unaffected; `console-operator -> [Data, Delegation]` least-privilege. Pure function + 2 unit tests; full crdb gate green. **D.3b-2 LANDED** (crdb main `4f861db2`): the live role feed -- a new `AigRoleResolver` (cdb-sed-graph) reads an enrolled peer's `AigAgentRecord.attributes["role"]` (credential-freshness-checked, fail-closed), wired into `resolve_acting` so the resolved role feeds `admit_wire_peer`; the `CDB_WIRE_ROLE_GRANTS` mkconfig knob authors `wire.enrolled_role_grants` (and fixed a real gap: `parse_wire_plane` did not map `delegation`). Full crdb gate green. **crdb engine side of D.3 is now complete.** **D.3a crdb half LANDED + CORRECTED** (crdb main `f8f9321e` then fix `54e39078`): the enrollment sink writes the enrolling ROLE to the agent record's `attributes["role"]`. The first cut read a `role:<name>` capability, which was **non-functional** -- capabilities are a fixed `AdminCapability` enum (`group -> AdminRole -> grant_for_roles`), so a `role:` capability can never be produced. The fix (product-owner chose "map from AdminRole") carries the enrolling `AdminRole` name through the context + artifact to the sink, so the wire role is a real role (e.g. `operator`), config-keyed as `CDB_WIRE_ROLE_GRANTS='operator=data,delegation'`. Server-derived, never client-asserted; two-layer least-privilege. Trade-off (accepted): the wire role is an admin-plane role, so `operator=data,delegation` grants Delegation to any operator-role device; the operator controls it via the group->role + grant maps. So the whole engine + enrollment chain now actually closes: IdP group -> `AdminRole::operator` -> agent-record role -> wire admission -> `[Data, Delegation]`. Remaining is **Console-side**: **D.3a-console** + **D.3c** (the MFA-gated live capstone). **D.3a-console RESCOPED (product-owner decision 2026-07-10): SERVICE-KEY ZTP with a Console-owned enroll client.** ZTP is required (permissions key off AIG enrollment, not static config), but TPM key custody is not; the earlier full-TPM path hit two walls (the unreachable `role:` capability -- fixed; and the sidecar cannot use a TPM-resident key -- so `IP-CONSOLE-00-SIDECAR-TPM` is PARKED as a future hardening option). The Console enrolls with a **software P-384 key** (still MFA + step-ca + **AIG registration**, so the `operator` role -> `[Data, Delegation]` chain works identically) that the sidecar reads as a PEM (`engine_cert`/`engine_key`); AWS-LC posture unchanged. `require_attestation` is a per-policy bool (default false), so attestation-less issuance is supported. `torch-enroll` is TPM-only + refuses software keys, so the Console builds its **own** small enroll client (software `KeystoreBackend` + the enroll protocol). **D.3a-console.1 LANDED:** the software keystore -- a new standalone Rust crate `enroll/` (`console-enroll`, like `sidecar/`; toolchain 1.96.0) with `src/keystore.rs` = P-384 keygen + PKCS#10 CSR + PKCS#8 key PEM on the AWS-LC module (`rcgen` forced onto its `aws_lc_rs` backend -- it defaults to `ring`); 4 unit tests (P-384 PKCS#8 export, well-formed CSR for the FQDN, empty-SAN, unique keys); wired into `scripts/ci.sh` step 11 (now loops sidecar + enroll) + its own `deny.toml`. **D.3a-console.2a LANDED:** the keystore now signs (raw ECDSA-P384 fixed `r||s` via `aws-lc-rs`, from the same key that drives the rcgen CSR/PEM) + exposes the public point -- the primitives DPoP needs; sign->verify test. **Protocol mapped (in the IP):** one-shot request/reply over bootstrap TLS + the 16-byte `cdb-wire` header + CBOR; messages `WireIdentityRequest`/`Offer` then `WireEnrollRequest`/`Response` (from the `cdb-wire`+`cdb-types` git-deps). **THE HARD CONSTRAINT:** a software key enrolls only via the `TokenAsserted` binding, which needs an RFC 9449 **DPoP `cnf`-bound token** (torch uses a bare token -> `NodeEstablished` -> forces TPM attestation -> refuses software); with default `require_attestation=false`, attestation is then never verified. **.2b LANDED:** the DPoP token-binding module (`enroll/src/token_binding.rs`) -- RFC 7638 canonical EC JWK + `jkt` thumbprint, and the RFC 9449 DPoP proof JWT (header `{alg:ES384, jwk, typ:dpop+jwt}`, claims `htm/htu/iat/jti/ath?`) signed ES384 by the keystore; hand-built JSON (no JSON dep), AWS-LC hashing. 4 tests: canonical/whitespace-free JWK + stable jkt, bad-point refused, the DPoP JWT is a 3-part JWT whose signature verifies, `ath` binds the token. **.2c-1 LANDED:** the RFC 8628 device-grant message modeling (`enroll/src/device_grant.rs`) -- `DeviceAuthorization`/`TokenSuccess` (serde), hand-built form-urlencoded request bodies (device-authorization + token-poll), and `parse_token_poll` -> `PollOutcome::{Pending, SlowDown, Approved, Denied}`; 5 fixture tests (form encoding incl. `%20`/percent-encoded grant, parsing, the OAuth error mapping). Deps +serde/serde_json. 13 enroll tests. Remaining: **.2c-2** the HTTPS transport (device-authorization POST + the DPoP-carrying token poll loop over rustls+aws-lc-rs) + **.2c-3** the bootstrap-TLS `cdb-wire` client (offer -> submit -> receive leaf; adds the `cdb-wire`+`cdb-types` git deps). Then **.3** provisioning wrapper (`engine_cert`/`engine_key`); D.4 wires enrollment into provisioning; D.3c is the MFA live capstone. |
| D.4 | INV-CONSOLE-DEPLOY-REPRODUCIBLE (end to end) | OPEN | -- | Installer entrypoint that lays down both processes (+ the D.3 identity) and a `validate` self-check that runs the CS.N proofs (admin floor + `/readyz` round-trip) and fails the install on a red leg; then the full stitched run from provisioned artifacts on a clean box. Depends on D.1-D.3. |
