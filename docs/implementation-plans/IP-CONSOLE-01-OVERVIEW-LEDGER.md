# IP-CONSOLE-01-OVERVIEW -- landing ledger

Per-PR landing record for `IP-CONSOLE-01-OVERVIEW.md` (the live connectivity graph, `TRD-CONSOLE-01`,
roadmap P1.3). One PR per roster row, a named slice of `INV-CONSOLE-OVERVIEW-LIVE`, the full
`scripts/ci.sh` green, branch-per-PR off local `main`, no-ff merge, push to `origin`, docs separate from
code. Reviewed with the maintainer before each merge.

Status: **IN PROGRESS -- O1.1 + O1.3 + O1.4 + O1.5 landed; grounded reshape (risk band, "Public" placeholder
VTZ, crdb CONNECTIVITY_GRAPH substrate) applied.** Prerequisites: P1.1 `IP-CONSOLE-12` (drawer) + P1.2
`IP-CONSOLE-09` (Logs) landed; O1.2 is SATISFIED cross-repo by crdb `IP-CONSOLE-CONNECTIVITY` (CN.1-CN.N).
Phase 0 foundation + the browsable `:8443` enabler are landed. The Overview is now a live, browsable surface
on the home route. NEXT = O1.6 (interaction: hover-highlight + tooltip + node -> the entity drawer, the
`overview.entityConnections` route + its prefetch, the two canonical <=3-click tasks).

| Step | Invariant | Status | Commit | Proof |
|------|-----------|--------|--------|-------|
| O1.1 | INV-CONSOLE-OVERVIEW-CONTRACT | LANDED | `36a3ff1` | Re-synced the vendored crdb wire schema (+ regenerated `wire-dto.ts` with `WireConnectivityQuery`/`Graph`/`ConnClass`/`ConnEdge`/`RiskBand` + the `ConnectivityGraph` request/reply variants). `@forge/contracts` `overview.ts`: `OverviewGraph`/`OverviewClassNode`/`OverviewEdge`/`OverviewRiskBand`/`RiskLevel`/`OverviewQuery` view models + the shared `toOverviewGraph`/`toRiskBand`/`toRiskLevel`/`toClassNode`/`toEdge` projection (typed against the generated DTO, fails closed on an unknown risk-level tag). `@forge/bindings`: `overview.graph` (LIVE, `connectivity_graph_v1`) + `overview.entityConnections` (LIVE, `entity_connections_v1`) + `overview.live` (PENDING, crdb Part B). Contract tests on both packages (projection + empty-tenant green + unknown-tag fail-closed; binding registration + LIVE/PENDING). |
| O1.2 | INV-CONSOLE-OVERVIEW-AGGREGATION (INV-CROSS) | SATISFIED (crdb) | crdb `f344aaff`/`d7884070` | The tenant-wide connectivity aggregation is the crdb `IP-CONSOLE-CONNECTIVITY` producer (`CONNECTIVITY_GRAPH`, CN.1-CN.N): LEG `ConnectsTo` roll-up -> source/dest class nodes + weighted edges + risk band, bounded + tenant-private + exposure-gated, live over `:7878`. No FC code; the O1.3 route consumes it. |
| O1.3 | INV-CONSOLE-OVERVIEW-BROKERED | LANDED | `6e1afab` | The BFF `overview.graph` route over `OperatorEngine`: `GET /api/overview/graph` brokers the tenant-wide `CONNECTIVITY_GRAPH` read (crdb `IP-CONSOLE-CONNECTIVITY`) and projects it via the shared `toOverviewGraph`. `@forge/wire`: the `ConnectivityGraph` request/reply added to the dispatch opcode table + a byte-exact CBOR encoder (Rust struct order, null `since`/`until`/`operator` omitted) proven against the crdb CN.2 golden. `wire-client`: `replyToConnectivityGraph` + the concrete `connectivityGraph`. `operator-engine`: brokers `connectivityGraph` (operator injected server-side, delegation recorded before the call). `overview.ts` resolver: fail-closed to `OverviewUnavailableError` on an unknown risk-band tag. Server route: session-gated (401), engine-gated (503), bounded `limit`, a tenant + bounds-scoped short-TTL cache (no cross-tenant reuse, keyed by `principal.tenant`; the graph reply has no commit version, so staleness is TTL-bounded), fail-closed mapping (503 unavailable / 403 refused / 502). Tests: resolver projection + empty-tenant + unknown-tag + millis->seconds; route 200/401/503/unavailable/cache-hit/tenant-isolation; operator-engine delegation; wire reply + byte-exact vectors. NOTE: `overview.entityConnections` is deferred to O1.6 -- `engine.entityConnections` already exists but has no consumer or view model yet; its route + `ConnectionList` projection land with the O1.6 hover-prefetch that consumes it (shipping an unconsumed route now would be a stub). |
| O1.4 | INV-CONSOLE-OVERVIEW-RENDERER | LANDED | `5f9ca8b` | `@forge/design` `OverviewFlow`: the three-column connectivity flow rendered as static SVG from the `OverviewGraph` view model (presentation only; the caller supplies the resolved graph). Left = source-class nodes (Users blue / Devices teal / AI Agents purple, `flow-*` tokens), middle = a single risk-colored "Public" zone (`status-good`/`caution`/`critical` by the risk band; NO per-VTZ score rings), right = destination-class nodes (amber `flow-objects`); ribbons flow source -> zone -> destination, colored by SOURCE class + stroke-width scaled by edge weight. Faint honeycomb ambient field (SVG hex `<pattern>`, `surface-border` at low opacity). Semantic-token color only (no hex; the hex-scan gate). Four honest states: loading skeleton (`aria-busy`), empty ("No connectivity observed", the zone still shows its green risk), and the ready flow. A11y: `role="img"` + an accessible name enumerating every source/destination class with its count + the zone risk (never color alone); unknown class tags title-cased + a muted lane fallback. 7 render/a11y/state tests (design suite 41 green). Full gate green. Static server-render preview delivered. |
| O1.5 | INV-CONSOLE-OVERVIEW-SURFACE | LANDED | `d399c30` | The real Overview surface replaces the F0.8 home placeholder. `useOverview` (TanStack Query) reads the live graph from `GET /api/overview/graph` (the O1.3 route). `OverviewSurface` mounts the `OverviewFlow` with the risk band as a header badge (Nominal/Elevated/Critical) + source-class **lane tabs** (All + one per source class) that filter the flow client-side (the engine has no lane parameter -- a VIEW filter over the real graph, not a data filter; `filterByLane` keeps the lane's node, its ribbons, and only the destinations they reach). The four honest states: loading skeleton + empty (the flow's own) and a hard-failure `ErrorState` with retry; the live-delta stream is O1.7, so an honest `StaleBanner` marks "not live yet". Registered in `routing/routes.tsx` (`overview` -> real surface), and `no-stub` contract updated (`REAL_SURFACES` += `overview`). `@forge/design` exports `sourceClassLabel` so the tabs label lanes identically to the flow. Shell frame test retargeted to prove the drawer host through the real Logs surface (the home node -> drawer trigger is O1.6). 6 surface tests + the console suite (37) + the full gate green. |
| O1.6 | INV-CONSOLE-3-CLICKS | OPEN | -- | The `overview.entityConnections` BFF route (`engine.entityConnections` -> a `ConnectionList` view model, brokered + bounded + fail-closed) lands here with its consumer; hover highlight + tooltip + drawer prefetch (`overview.entityConnections`); click entity -> the drawer (`TRD-CONSOLE-12`) + Quick Actions; the two canonical <=3-click tasks by contract (zone nav deferred with the real VTZ store). |
| O1.7 | INV-CONSOLE-LIVE | OPEN | -- | `overview.live` applies deltas in place (< 2 s) over the F0.6 live-store; stale/reconnect/resync; push-stream swap-in later. |
| O1.N | INV-CONSOLE-OVERVIEW-COMPLETE | OPEN | -- | Playwright E2E of the flagship tasks + the fixtureless empty-tenant render + a < 2 s live delta; all `TRD-CONSOLE-01` Section 8 acceptance rows green. |

### Overview graphic REDESIGN (Sankey) -- design LOCKED 2026-07-14 (supersedes the O1.4/O1.5 "Public placeholder" renderer)

A user-driven redesign into a true three-column Sankey: source-class rings -> up to 3 VTZ corona nodes (each with its OWN **detection-driven** risk band) -> destination-category rings with all apps on one shared arch. Ribbons dissolve into every node (radial-hole mask), sorted ports (no crossings), left lane-colored / right amber at ~0.6 opacity. The three demo VTZs (`Demo.Users.Public` = all other traffic, `Demo.Private.Agent` = Demo Agent, `Demo.Public.Agent` = Claude Code + Codex) become permanent live config. Design locked via a static prototype; realized in these PRs:

| Step | Invariant | Status | Commit | Proof |
|------|-----------|--------|--------|-------|
| RD.1 | INV-CONSOLE-OVERVIEW-SANKEY-CONTRACT | LANDED | `123078b` | `@forge/contracts` `overview.ts` Sankey v2 view model (additive; old `OverviewGraph` kept until the surface migrates): `OverviewSankey` = `sources` / `vtzs` (per-VTZ detection-driven `risk`) / `destinations` (with `apps` + `moreCount`) / two-stage `sourceEdges` (source->VTZ) + `destEdges` (VTZ->dest). Helpers: `overviewVtzPage`/`overviewVtzPageCount` (max 3 per page, "swipe for more"), `overviewHighlight`/`sourceEdgeKey`/`destEdgeKey` (hover-a-destination -> the contributing source->VTZ->dest path set, for the linked-highlight filter). 3 contract tests (paging clamp, highlight of private-apps = Public only, highlight of network = Public + Public.Agent). Full gate green. NEXT = RD.2 rebuild `OverviewFlow` renderer to this model. |
| RD.2 | INV-CONSOLE-OVERVIEW-SANKEY-RENDERER | LANDED | `ddc0beb` | `@forge/design` `OverviewSankeyFlow` -- the locked Sankey as a tokenized React component from `OverviewSankey` (additive; the old `OverviewFlow` stays until the surface migrates in RD.4). Source-class dotted-mist rings (`)` arch, lane color, dynamic radius by count) -> up to 3 VTZ **corona** nodes (per-VTZ risk tint, stacked name, spin) -> amber destination rings (`(` arch) with every app on ONE shared arch (text-only, 5 + "+N more"). Ribbons two-stage (source->VTZ lane / VTZ->dest amber, ~0.6 opacity), dissolve into every node via one radial-hole `<mask>`, **sorted ports** (order by the other endpoint's y -> no crossings). Hover a destination -> `overviewHighlight` dims non-contributing ribbons + fades non-path VTZs/dests. Semantic-token color ONLY (var()/color-mix in gradient stops; black/white keywords for the mask; new `text.soft` numeric token) -- hex gate green. Loading skeleton + empty ("no connectivity observed"); a11y `role="img"` name enumerates sources/zones+risk/dests. 5 tests (46 design suite). SSR preview delivered. NEXT = RD.3 crdb VTZ dimension. |
| RD.3 | INV-CONSOLE-CONNECTIVITY-VTZ (crdb INV-CROSS) | OPEN | -- | crdb `CONNECTIVITY_GRAPH` gains the VTZ dimension: source->VTZ->dest via VTZ assignment config, per-VTZ risk from detections; seed the 3 demo VTZs. |
| RD.4a | INV-CONSOLE-OVERVIEW-SANKEY-BROKERED | LANDED | `68fc32a` | Re-synced the vendored crdb wire schema (regen `wire-dto.ts`): `WireConnectivityGraph` gains `vtzs`/`source_edges`/`dest_edges` + the 3 structs. `@forge/contracts` `toOverviewSankey` projects the two-stage wire graph to `OverviewSankey` (fail-closed on unknown VTZ risk; no apps yet -> `moreCount = count`). BFF `resolveOverviewSankey` + `GET /api/overview/sankey` (shared serve helper, tenant+bounds cache keyed by kind); additive (flat `overview/graph` kept). Contract + BFF resolver/route tests; full gate + e2e green. |
| RD.4b | INV-CONSOLE-OVERVIEW-SANKEY-SURFACE | OPEN | -- | Rewire `OverviewSurface`: `useOverview` reads `/api/overview/sankey` -> `OverviewSankey`; mount `OverviewSankeyFlow` (3-VTZ swipe paging + hover-filter + click-to-fan-out); retire the old `OverviewFlow`/`OverviewGraph` path. Run `pnpm exec playwright test` before merge. |
| RD.5 | INV-CONSOLE-OVERVIEW-APPS-DNS | OPEN | -- | DNS-resolved common app names (torch capture -> resolve -> crdb enrichment); then generate real traffic (demo agents + users/devices) so the graph draws out live. |
