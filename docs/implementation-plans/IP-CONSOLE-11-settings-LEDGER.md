# IP-CONSOLE-11-SETTINGS -- landing ledger

Plan: `IP-CONSOLE-11-settings.md` (TRD-CONSOLE-11 Settings, amended Section 9). Created WITH the plan
(2026-09-24) per the ledger discipline: every step's row is updated (status + commit hash) in the same
session its PR merges, and the Resume-here section is rewritten at every merge. A stale ledger is a
defect. The paired engine ledger is crdb `IP-CONSOLE-SETTINGS-WIRE-LEDGER.md`.

## Resume here (rewrite at every merge)

- **State (2026-09-25, ~01:45 UTC):** crdb SET.3 + SET.5 merged + deployed; **ST.9 LANDED `3ea169c` + INSTALLED. **ST.N LANDED `1705e9e` + INSTALLED: THE SETTINGS BUILD-OUT IS COMPLETE.** After it: the Settings build-out is complete except ST.11 (blocked on crdb SET.6 engine verbs, each its own plan) and the Failover & DR tab (absent until the engine serves regions); then IP-AISOC-STEP1 D and E.** Earlier (2026-09-24, ~23:40 UTC): ST.2b merged + installed; crdb SET.1b (typed identity values) merged + deployed; **ST.3 + ST.4 LANDED `23cf5f8` + INSTALLED; crdb SET.4 merged + deployed (`10a1f986`); **the report tabs (ST.5a / ST.6 / ST.8 / ST.10) LANDED `7d6a86e` + INSTALLED; **ST.5b LANDED `84dd9de` + INSTALLED (validate 8/8, live-proven on the P-384 floor)**; NEXT (was) = crdb SET.3 (dual control on the wire) + SET.5 (history) for ST.9.** Earlier: crdb SET.1 + SET.2 merged and deployed; SET.2b on branch. **Corrections recorded:** the admin assignments and the SSO map are boot-bound (RBAC / Federation show them read-only); the detection watermark bounds are pending (Policy shows them read-only). ST.3 / ST.4b now wait only on SET.1 (deployed). Prior: **ST.0 LANDED with the plan** (the TRD amendment). The SOC tab is LIVE from
  IP-CONSOLE-03 S3.18. **ST.1 LANDED + INSTALLED; was ON BRANCH `feat/console-11-st1-settings-shell` (2217dc9), AWAITING REVIEW; NEXT = ST.2** (editing on the Configuration tab: knob edits + the typed section forms over SET.2 / SET.2b). ST.4a (the Federation tab's connector panel
  mount) needs no engine work and may land any time.

## Roster (ForgeCentral)

| Step | Acceptance | Status | Commit | Notes |
|------|-----------|--------|--------|-------|
| ST.0 | TRD 9 | LANDED (2026-09-24, with the plan) | -- | TRD-CONSOLE-11 Section 9: the engine's real admin surface, the corrected tab set (SOC + Configuration added; FIPS toggle removed; HA / DR / rotation PENDING with owning work), Step 1 scope, added acceptance + failure semantics. |
| ST.1 | 9.1, 9.4 | LANDED (merged `78e3aaf`, 2026-09-24; installed, validate 7/7) | `2217dc9` | Schema re-vendored from crdb `2cf0a451`; `SettingsRead` wired in `@forge/wire` dispatch + payload. **Contracts** `settings.ts`: `SettingRow` / `SettingsView` / `SectionValues` + `toSettingsView` (fail-closed on a refusal or an origin outside knob / section / env / const; section values null unless every field is present), `settingSourceLabel` (committed at version N / fail-closed default / node environment at boot / compile-time constant), `settingApplyClass` (from the registry's own `live_apply` text: live / boot-bound / pending / fixed). **BFF** `engine/settings.ts` `resolveSettings` + `GET /api/settings[?surface=]` (surface must match `[a-z_]{1,64}`; tier refusal 403; nothing cached); `settingsRead` on client / operator engine (delegated) / wire client. **SPA** the Settings `TabStrip` shows only tabs with LIVE bindings (SOC, Configuration); Configuration is READ-ONLY here: a surface picker over the engine's surfaces and a `DataTable` of each surface's settings (setting, value as the engine rendered it or `not in the committed document`, source, applies, definition + bound), the version line and the dual-control note; tier below Admin / SecurityAudit = an honest empty state. Tests: contracts 4, BFF 2, SPA 3 (tabs from live bindings; rows with source + apply class across surfaces; tier-below). |
| ST.2a | 9.2 Configuration | LANDED (merged `4417a45`, 2026-09-24; installed, validate 7/7) | `fd2aad6` | Knob edits over crdb SET.2. `SettingsCommit` wired in `@forge/wire` dispatch with a FULL encoder (edits + every `WireSectionPatch` field, so ST.2b is UI only). **Contracts:** `SettingsCommitRequest` + `toSettingsCommitRequest` (non-empty, <= 64 edits, keys `[a-z_][a-z0-9_.]{0,127}`, values <= 256 chars), `toWireSettingsCommitFields`, `SettingsReceipt` + `toSettingsReceipt` (never null; an unknown cause kept as `other` with its tag), `refusalCauseLabel` (each SET.2 cause in words), `isKeyEditable` (engine `editable` AND a knob AND not `CapabilitySet`). **BFF:** `settingsCommit` on client / operator engine (delegated) / wire client; `resolveSettingsCommit`; `POST /api/settings` (400 malformed; receipt 200). **SPA:** a Change column: Edit on key-editable rows only, `read-only` elsewhere and everywhere under dual control; staged batch (`N staged changes; the engine applies them together or not at all`), Commit behind a `ConfirmDialog` listing `key: old -> new`, the receipt verbatim (`Committed at version N. Applied live.` or the restart list; refused: every cause in words + detail and every violation), a refused batch stays staged, a commit re-reads the settings. Tests: contracts 3, BFF 1, wire dispatch case, SPA 3 (stage -> confirm old/new -> post -> receipt; refused causes + boot-bound read-only; dual control offers no edit). |
| ST.2b | 9.2 Configuration | LANDED (merged `e9b69e2`, 2026-09-24; installed, validate 7/7) | `99a8d96` | **Contracts:** `SectionPatch`, `ADMIN_CAPABILITIES` (the 12 engine capability names; a stale list fails closed at the engine), `toSectionPatch` (lists <= 256, ceilings from `CLASSIFICATION_TAGS`, non-empty ids / sources / formats, LUG integers >= 0 within u32 / u16), `toSettingsCommitRequest` now takes edits and / or sections (at least one), `toWireSettingsCommitFields` maps sections. **SPA** `SettingsSectionForms.tsx`: one form per LIVE section whose registry row the engine marks `editable` (dual-control checkboxes; egress rows with id + ceiling, add / remove, commit disabled on a blank id; LUG two toggles + six bounds; decoder families one per line; source-format `source=format` per line, commit disabled on a malformed line; narrative model ref, empty = unbind); each form starts from the engine's typed `sectionValues`, re-mounts on a new committed version, commits ONLY its section behind a `ConfirmDialog` naming it, and shows the shared `GovernedReceipt` (moved here from the surface); no forms under dual control or when the read carries no section values. Tests: contracts 2 (narrow + compile; malformed refused), SPA 4 (forms only for editable sections; dual control posts only its section after a named confirm + receipt; egress add with blank-id guard + model unbind; none under dual control). |
| ST.3 | 9.2 RBAC | LANDED (merged `23cf5f8`, 2026-09-24; installed, validate 7/7) | `75b752b` | **Engine rider first:** crdb SET.1b (`aa45e2eb`, deployed) serves `identity_values` typed, because the `identity.admins` text rendering is ambiguous (a free-form identity carrying `;x=[Root]@...` reads as a forged second admin); schema re-vendored. **Contracts:** `IdentityValues` on `SettingsView.identity` (null when the engine does not send it), `adminRoleLabel` (an unknown role shown verbatim), `ConsoleRbacView` + `toConsoleRbacView` (fail-closed on an unknown role or shape). **BFF:** `rbacConfigView` (sorted by key; a global admin's tenant shown as none) and `GET /api/settings/console-rbac`: 401 without a session, 403 `Role` for anything but `global-admin` (the map names every tenant and subject), 200 otherwise. **SPA** `SettingsIdentityTabs.tsx` `RbacTab`: engine admin assignments table (identity verbatim, role badges, clearance) with `Source: committed at version N. Read-only here: <live_apply>. Change it with <change_via>, then restart the node.`; the Console role map panel (IdP group grants, subject fallback grants, default tenant) labelled installer configuration, or `Global admin required`; a link to Users for tenant operators. Tests: contracts 2, BFF 2 (route 401 / 403 / 200; projection), SPA 2 (the tricky identity is ONE row, verbatim, no edit control; non-global admin + tier-below). |
| ST.4 | 9.2 Federation | LANDED (merged `23cf5f8` with ST.3; installed, validate 7/7) | `75b752b` | (a) the External IDAM tab moved from `UsersSurface` into `IdamConnectorsPanel.tsx` (verbatim, exported) and mounted by both Users and Settings: one component, one fetch path; (b) the SSO group to admin role map read-only from the typed values with the boot-bound source line. Test: SPA 1 (SSO map with role labels; the connector panel mounts and reads its connectors). |
| ST.5a | 9.2 Security | LANDED (merged `7d6a86e`, 2026-09-25; installed, validate 7/7) | `591b18e` | **Plumbing (all report tabs):** schema re-vendored from crdb `10a1f986`; `SettingsReports` in `@forge/wire` dispatch + encoder (`request_id, reports, operator?`); `settingsReports` on client / wire client / operator engine (delegated); contracts `SETTINGS_REPORT_NAMES`, `SettingsReportsView` + `toSettingsReportsView` (null on refusal), `toSettingsReportNames` (non-empty, known, de-duplicated); BFF `resolveSettingsReports` + `GET /api/settings/reports?names=` (400 before the engine on an empty / unknown name; 403 tier; nothing cached); SPA `useSettingsReports(names)` + `SettingsReportTabs.tsx` (`WithReports`: loading / error / tier-below / `This node runs no admin endpoint`). **Security tab:** asks `connectivity,security,egress`; admin endpoint facts, server security facts (audit chain `Verified` / `Failed verification`), egress destinations; edits pointed at Configuration; the session key exchange stated unavailable with ST.5b named. |
| ST.5b | 9.2 Security | LANDED (merged `84dd9de`, 2026-09-25; sidecar rebuilt + installed, validate 8/8; LIVE: an `openssl s_client -groups secp384r1` session held through :8443 was found at the lookup under its tunnel port as `secp384r1`; the box's OpenSSL 3.0 cannot offer X25519MLKEM768, so the hybrid live case rests on the sidecar end-to-end test) | `151c18f` | **Design change from the plan (stated): no header.** The terminator is a byte tunnel that never parses HTTP; injecting a header would mean an HTTP parser in the crypto boundary (keep-alive, pipelining). Instead: `session_service.rs` `SessionRegistry` (loopback source port -> group; `SessionGuard` removes on tunnel end) and an optional loopback NDJSON `SessionService` (`{"port":N}` -> `{"group":...|null}`, 256-byte cap, `deny_unknown_fields`, routable bind refused); `AdminTerminator::with_sessions` registers `negotiated_key_exchange_group` (named `X25519MLKEM768` / `secp384r1` / `other`) BEFORE the first forwarded byte. Config `session_addr` (optional, loopback-only); `main.rs` serves optional legs through one `select!` via `optional()` instead of a 2^n match. **BFF** `session-client.ts` `lookupSessionGroup` (fail-closed: refusal, garbled line, close, timeout) and `GET /api/settings/security-session` keyed by `req.socket.remotePort` (the browser supplies nothing): `negotiated` / `not-tunnelled` / `unconfigured`, 503 on a failed lookup. **SPA** the Security tab's `SessionKx` panel (hybrid = good badge, P-384 floor = caution). **Deploy:** `provision-sidecar.sh` writes `session_addr` (SESSION_PORT 8792), `install.sh` writes `FC_SIDECAR_SESSION_PORT`, `validate.sh` [2c] proves the lookup answers `{"group":null}` for port 1 (validate becomes 8/8). Tests: sidecar 5 (group names; live tunnel found + forgotten; malformed / unknown-field / oversized refused; routable bind refused; END TO END through the terminator for BOTH groups with the upstream reporting its peer port; config optional + loopback-only), BFF 4 (client ask/null; fail-closed x3; `remotePort` IS the client's local port; route unconfigured + negotiated), SPA 1 (+ the Security test now asserts the hybrid label). |
| ST.6 | 9.2 KeyLock | LANDED (merged `7d6a86e`, 2026-09-25; installed, validate 7/7) | `591b18e` | key-issuing report (enabled, dual control, validity); signing-key ids + rotation stated unavailable (crdb SET.6c); no Rotate control. |
| ST.7 | 9.2 Policy | PLANNED (waits crdb SET.2b) | | model ref editable; watermarks / retention / served models read-only |
| ST.8 | 9.2 Observability | LANDED (merged `7d6a86e`, 2026-09-25; installed, validate 7/7) | `591b18e` | telemetry report facts + the committed `observability` rows from the governed read (edited on Configuration, no second form); exporter configuration stated boot-only (crdb SET.6d). |
| ST.9 | 9.4, 9.5 | LANDED (merged `3ea169c`, 2026-09-25; installed, validate 8/8) | `1056d3e` | Schema re-vendored from crdb `dcfebc93`. **Wire/BFF:** five ops through `@forge/wire` (propose reuses the commit encoder), client / wire client / operator engine (delegated); routes `POST /api/settings/propose`, `GET /api/settings/approvals` (403 tier), `POST /api/settings/approvals/<id>/approve`, `GET /api/settings/history?limit=` (1..100, 400 otherwise), `POST /api/settings/rollback {to}`; malformed input 400 before the engine; refusals are 200 receipts. **Contracts:** `SettingsReceipt` gains `proposal` + `approvalRefused` (`approvalRefusalLabel` in words), `SettingsProposalReceipt`, `PendingProposal` / `toPendingProposals`, `SettingsHistoryView` / `toSettingsHistoryView`, `toPositiveId`, `toHistoryLimit`. **SPA:** `useGovernedChange(propose)`: under dual control the knob table and the section forms PROPOSE (confirm says so; `ProposalReceipt` names the id); the dual-control note points at the Changes tab. New **Changes** tab (`SettingsChangesTab.tsx`): pending approvals from both planes (proposer, time, changed keys; a stale proposal shows a Stale badge and no Approve), Approve behind a confirm naming proposer + keys + the self-approval rule, refusals in words; history (version, committed by or `not recorded`, time, changed keys, `current` on the head, Roll back behind a confirm, the incomplete-page note), a rollback under dual control shows its proposal id. **Not changed, stated:** the SOC tab's own commit still refuses under dual control (C.9c `SOC_SETTINGS_COMMIT` has no propose; its tiers are editable as `soc.*` knobs on Configuration, which propose). Tests: contracts 3, BFF 1 (every route: 400s before the engine, then each op with its argument), SPA 5 (knob propose; section propose; approvals list + stale + confirm + refusal; history + rollback proposal; tier-below). |
| ST.10 | 9.2 HA / DR / FIPS | LANDED (merged `7d6a86e`, 2026-09-25; installed, validate 7/7) | `591b18e` | HA & Topology: the server report (shards, serving, durable, maintenance cadence, frame ceiling, version); cluster leader / lag / Rotate Leadership / Test Quorum Loss and regions + shard placement stated unavailable (crdb SET.6a). FIPS Mode: provider + FIPS module linked (No on this build) + the build-time procedure; no toggle. **Failover & DR is ABSENT from the tab strip**: the engine serves no regions or DR read, and a tab of only absences would be a stub (it appears with SET.6b or a regions read). Tests (all report tabs): contracts via BFF, BFF 2 (resolver names + projection + refusal; route 400s before the engine, de-dup, 200), SPA 5 (Security asks exactly its three reports; failed chain, no admin plane, tier-below; KeyLock; Observability; HA + FIPS offer no control). |
| ST.N-fix | 9.4 | ON BRANCH `fix/settings-delegated-tier`, FULL gate green (incl. e2e 40/40 + audit), AWAITING REVIEW | `6caed45` | Paired with crdb SET.4b. **Found by the operator's signed-in walk:** every Settings tab refused a global admin; the engine never learned the operator's tier. `principalFromSession` sets `settingsTier` (the session's own tier) for a `global-admin` session ONLY (a tenant-admin also holds Admin but gets none); `operator-engine.ts` `settingsDelegation()` sends it as `settings_tier` on the ten Settings calls only; `payload.ts` `applyOperator` encodes it when present. Every other call keeps the plain delegation. Tests: BFF 2 (only a global-admin session carries a Settings tier; it rides the Settings call and NOT the same admin's non-Settings call). Also fixed: the ST.N sweep read the tab names before the first paint (flaky under a loaded run); now a web-first `toHaveText`, 36 / 36 on repeat. **Owed:** the operator's signed-in walk after deploy. The full permission map is DEFERRED (crdb `DEF-RBAC-PERMISSION-MAP`, operator ruling 2026-09-25). |
| ST.11 | 9.2 | BLOCKED (crdb SET.6: TRD-07 cluster + DR verbs, TRD-04 rotation verb; each its own plan) | | |
| ST.N | 7, 9.4 | LANDED (merged `1705e9e`, 2026-09-25; installed, validate 8/8; FULL gate incl. e2e 40/40) | `7333a6b` | `apps/console/e2e/settings.spec.ts` (Chromium, BFF mocked at the network boundary as the BFF emits each view): (1) Overview -> Settings -> Configuration -> Edit (3 clicks), stage, confirm shows old -> new, nothing posted before the confirm, the receipt `Committed at version 13`, and a boot-bound row has no Edit; (2) under dual control the same edit PROPOSES (`Proposal 7 recorded`), the Changes tab lists it, Approve behind a confirm stating the self-approval rule, committed receipt, the posts are exactly propose then approve, the history names committers; (3) the NO-STUB SWEEP: the ten tabs in order, each shows its engine value (tiers, version, the admin identity, the SSO group, history, the hybrid session group, `1 d` validity, the OTLP address, the cadence, the provider), RBAC and Federation state `then restart the node` and offer no edit / commit / propose control, no placeholder text anywhere, every request is a real BFF route (an allow-list; anything else fails), nothing posted; negative-checked (a wrong expectation fails the sweep); (4) the tier below: the honest empty state on every engine tab. **OpenAPI:** `SETTINGS_PATHS` documents all ten Settings routes (responses 200 / 400 / 401 / 403 / 502 / 503 as each applies) and a BFF test proves the document and the router agree (each documented operation answers 401 without a session, never an unrouted 404 / 405; the documented set equals the table). **Drift fixed:** `shell.spec.ts` expected 11 rail links; S3.16 removed Agent Ops + Network Ops and the spec rotted unseen because the box gate ran `--skip-e2e`; now 9 links, and the freshness check navigates to Settings. **Live on the box:** the deployed BFF answers every documented route 401 without a session (all ten claimed). The engine legs are proven by crdb SET.1..SET.5's capstones against a booted node and the shipped `cdb-actl` (this ST.N absorbs crdb SET.N). **Owed (operator):** a signed-in browser walk of the ten tabs on the box (OIDC login is interactive). |

## Deferred / owed

- The admin-plane negotiated-group header (sidecar -> BFF) is the one Console-side engine-free
  prerequisite for ST.5; it rides with ST.5.
- CLOSED in ST.N: the Settings routes are in the BFF OpenAPI document with a drift test. The OTHER `/api/*` surfaces (SOC, Users, Objects, Policies, VTZ, Logs, Overview) are still undocumented there: a Console-wide gap outside this plan.
- The box's FC gate runs `--skip-e2e` by habit; ST.N found a rotted spec that way. Run the full gate (e2e works on the box: Chromium is installed) before merging any SPA change.
- The Console's own operator roles are installer configuration (`rbac.ts`); moving them into the
  engine's `admins` model is out of scope here and belongs to the identity plan.
