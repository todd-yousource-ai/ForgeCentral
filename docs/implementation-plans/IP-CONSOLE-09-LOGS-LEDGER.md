# IP-CONSOLE-09-LOGS -- landing ledger

Per-PR landing record for `IP-CONSOLE-09-LOGS.md` (the decision/audit LOG stream, `TRD-CONSOLE-09`,
roadmap **P1.2 -- the second Phase-1 surface**). One PR per roster row, a named slice of
`INV-CONSOLE-LOGS-REAL`, the full `scripts/ci.sh` green, branch-per-PR off local `main`, no-ff merge,
push to `origin`, docs separate from code. Reviewed with the maintainer before each merge.

Status: **COMPLETE -- LG.1-LG.N all landed.** The Logs surface is live end to end (table + engine-side filters + EXPLAIN + row->drawer drill-in + live tail + audited export), browser-capstoned; DR.N (the drawer capstone) is closed by LG.N. The crdb LOG substrate is live (`IP-CONSOLE-LOG-QUERY` LQ.1-LQ.3:
`LOG_QUERY` + `LOG_EXPLAIN` over `:7878`), so `logs.query`/`logs.explain` bind LIVE. Prerequisite: P1.1
`IP-CONSOLE-12` (drawer, for the row click) -- functionally complete. This
surface establishes the LOG substrate the Overview (P1.3) aggregates. Phase 0 + the `:8443` enabler are
landed.

| Step | Invariant | Status | Commit | Proof |
|------|-----------|--------|--------|-------|
| LG.1 | INV-CONSOLE-LOGS-CONTRACT | LANDED | (this PR) | `@forge/contracts`: resynced the vendored wire DTO schema to crdb (adds `WireLogQuery`/`WireLogExplain`/`WireDecisionDetail`) + regenerated the DTOs (drift gate green). New `logs.ts`: `LogRow` (projection of `WireDecisionRow`), `LogDetailView` (projection of `WireDecisionDetail` + the derived acting-entity ref for the LG.5 drill-in), `LogQueryFilter`, `LogPage`. Grounded-column note: entity/category/VTZ not emitted on a decision row (drill-in / Forge-side), trust delta removed -- those render empty, never fabricated. `@forge/bindings`: `logs.query` + `logs.explain` LIVE against crdb `LOG_QUERY`/`LOG_EXPLAIN` (IP-CONSOLE-LOG-QUERY LQ.2/LQ.3, landed); `logs.tail` + `logs.export` honest PENDING (crdb Part B push; crdb LQ.4 export). Tests: contracts `logs.test.ts` (6) + bindings `logs.*` block; F0.8 shell no-stub test updated to allow the `logs.*` contract. Full `scripts/ci.sh --skip-net --skip-e2e` green. |
| LG.2 | INV-CONSOLE-LOGS-QUERY (the LOG substrate) | LANDED | (this PR) | `logs.query` + `logs.explain` LIVE end to end through the BFF over `OperatorEngine` against the crdb `LOG_QUERY`/`LOG_EXPLAIN` verbs. `@forge/wire` dispatch routes `LogQuery`/`LogExplain` to the QuerySubmit opcode; `wire-client` `replyToDecisionDetail` + `logQuery`/`logExplain` (query reuses `Reply::DecisionList`); `CrucibleClient` + `OperatorEngine` methods (operator delegation injected server-side + recorded); `engine/logs.ts` `resolveLogQuery` (filter millis->seconds, project rows) + `resolveLogExplain` (full detail + derived acting-entity for the LG.5 drill-in); `GET /api/logs` (bounded filter from the query string) + `GET /api/logs/explain/<id>` (401 no session, sanitized 403 query-gate refusal, non-oracle 404 absent). Shared `toDecisionStatus` extended to grade posture tags (escalate->denied/candidate->flagged/observe-only->pass). Tests: server routes + operator-engine delegation injection; untouched mocks use the `unused` double. Full `scripts/ci.sh --skip-net --skip-e2e` green. |
| LG.3 | INV-CONSOLE-LOGS-TABLE | LANDED | (LG.3a + LG.3b) | **LG.3a** (design): the shared `DataTable` primitive -- typed columns, horizontal scroll, optional interactive rows (click/keyboard + accessible label + hover-prefetch), honest empty node; `.fc-visually-hidden` util; all `--fc-*` tokens. NOT virtualized (single bounded page today; deep paging is a named crdb deferral, so windowing is premature -- lands behind the same props shape). 5 design tests. **LG.3b** (surface): `useLogs` (TanStack query, filter IS the query key -> engine-compiled refetch) + `LogsSurface` (search/confidence/outcome/time-range controls -> `LogQueryFilter`; DataTable with the decision color-cue badge; loading skeleton / error+retry / honest empty echoing the active filters); routed `/logs` to the real surface. 4 surface tests + updated the F0.8 no-stub test (Logs is a real surface). Rows inert here -- the row->drawer drill-in is LG.5. Full `scripts/ci.sh --skip-net --skip-e2e` green. |
| LG.4 | INV-CONSOLE-LIVE | LANDED | (this PR) | `useLogs` polls the recent window (`refetchInterval` 2s TUNE) with `keepPreviousData`, so new decisions appear at the top in place (no flash on a tick; stale rows stay while a failed poll reconnects); `live=false` pauses for a historical view. A Live badge with the honest state (Live / Reconnecting = a failed background poll that kept its rows / Paused) + a Pause/Resume control (`aria-pressed`); a first-load failure with no data is still the full ErrorState. Honest v1 polling; the real push-stream (crdb Part B / F0.6 SSE) swaps in behind the same hook. 3 tests (pure `liveIndicator` all-states + Live default + Pause/Resume). Full `scripts/ci.sh --skip-net --skip-e2e` green. |
| LG.5 | INV-CONSOLE-3-CLICKS | LANDED | (this PR) | Row interaction, the first real drawer open-site. `useLogExplain`/`fetchLogExplain`/`logExplainQueryKey` (the decision EXPLAIN read, cached by id). The Decision cell is a button that toggles that decision's EXPLAIN rationale inline (finding/rule/ATT&CK+tactics/scope/evidence + a "View acting entity" button; stops propagation). Row activation resolves the acting entity from the cached EXPLAIN detail and opens the entity drawer (`drawer.openEntity`, IP-CONSOLE-12 reused); a decision with no acting entity opens its EXPLAIN instead; row hover prefetches. This is the trigger the drawer's DR.N was deferred to. 2 interaction tests (decision->rationale; row->drawer fetching /api/entity); the LG.3 tests now render inside a DrawerHost. `PENDING`: the row->Rewind deep-link (Phase 2). Full `scripts/ci.sh --skip-net --skip-e2e` green. |
| LG.6 | INV-CONSOLE-LOGS-EXPORT | LANDED | (this PR) | The audited export, end to end, over crdb `LOG_EXPORT` (IP-CONSOLE-LOG-QUERY LQ.4, landed). Resynced the vendored wire schema (adds `WireLogExport`/`WireLogExportEffect`) + regenerated; new `LogExportRequest`/`LogExportView`. `@forge/wire` dispatch routes `LogExport` to the QuerySubmit opcode; `wire-client` `replyToLogExported` + `logExport`; `CrucibleClient` + `OperatorEngine` methods (operator delegation injected + recorded); `engine/logs.ts` `resolveLogExport` (builds `WireLogExport` from the filter + a commandId, projects rows + receipt). `POST /api/logs/export` (bounded body; 401/400/403 sanitized). `logs.export` binding flipped PENDING -> LIVE (`log_export_v1`); only `logs.tail` (push) remains a named deferral. SPA: an Export button -> POST -> download the audited rows as JSON + show the audit receipt (export id + commit version). NEVER a client-assembled CSV -- the rows come from the audited op whose receipt is on the chain. Tests: server export route + operator-engine delegation + console export (receipt shown, download triggered, POST carries commandId + filter). Full `scripts/ci.sh --skip-net --skip-e2e` green. |
| LG.N | INV-CONSOLE-LOGS-COMPLETE | LANDED | (this PR) | `apps/console/e2e/logs.spec.ts` -- the Playwright tier-4 journey, BFF mocked at the network boundary (disjoint RegExp routes). 2 specs, both green: (1) table over decisions -> engine-side filter recompute (a /api/logs read carries `confidence=HIGH`) -> decision cell -> EXPLAIN rationale -> row -> entity drawer for the acting entity -> audited export receipt; (2) the live tail surfaces a newly-seeded decision at the top under the poll. All `TRD-CONSOLE-09` Section 8 rows green. **The row->drawer open is the trigger the drawer's DR.N was deferred to, so this capstone CLOSES DR.N.** Gate green (e2e stage validated directly via `playwright test`). |
