# IP-CONSOLE-12-ENTITY-DRAWER -- landing ledger

Per-PR landing record for `IP-CONSOLE-12-ENTITY-DRAWER.md` (the shared detail + quick-actions panel,
`TRD-CONSOLE-12`, roadmap **P1.1 -- the first Phase-1 surface**). One PR per roster row, a named slice of
`INV-CONSOLE-DRAWER-REAL`, the full `scripts/ci.sh` green, branch-per-PR off local `main`, no-ff merge,
push to `origin`, docs separate from code. Reviewed with the maintainer before each merge.

Status: **IN PROGRESS -- DR.1, DR.2 landed; DR.1 REVISED (trust score removed).**

**DR.1 revision (2026-07-11):** Trust Score removed (legacy of the old architecture). `HeaderView` is now identity + `EntityStatus` (active/suspended/compromised/unknown, from the engine agent status); the drawer header drops `ScoreRing`/`Sparkline` for a status Badge (the primitives are retained for other uses). Bindings reclassified against grounded crdb reality: `entity.header`/`entity.info` LIVE over LIST_AGENTS (ER.1), `entity.recentDecisions` LIVE over ENTITY_DECISIONS (ER.2c); `entity.zones`/`entity.effectivePolicies` -> PENDING (Forge, no crdb store). Full TS gate green. No surface prerequisite (built first; Logs P1.2 + Overview
P1.3 reuse it). Phase 0 foundation + the browsable `:8443` enabler are landed.

| Step | Invariant | Status | Commit | Proof |
|------|-----------|--------|--------|-------|
| DR.1 | INV-CONSOLE-DRAWER-CONTRACT | LANDED | (this PR) | `@forge/contracts/src/entity.ts`: the entity ref (kind-branded union), the six section view models (header+trust, info, zones, capabilities, effective policies, recent decisions) as PROJECTIONS of the engine DTOs (`WireDecision`/`WireAuditEntry` + branded ids), the `SectionState<T>` envelope (ok/empty/not-applicable/pending/unauthorized/error, TRD-12 Sec 6), the aggregate `EntityDetailView`, and the quick-action command shapes (idempotent `commandId`; `IsolateEffect.enforcementActive=false`, AG.7 off). `@forge/bindings`: registered the 10 `entity.*` bindings -- 5 CrucibleQL section reads LIVE, `entity.capabilities` PENDING (torch DR.4 Construction Report read binding), `entity.isolate` LIVE audited command, `reassignZone`/`remediation`/`fullReport` PENDING (named gating surface). **Design note:** this puts the first tracked PENDING deferrals into the committed manifest, so `assertReleaseReady` now correctly gates a release; the F0.4 contract test inverted from "release-ready" to "dev-valid, only blocker is the named PENDING set" (the intended "PENDING passes DEV, fails release" behavior). Updated the F0.8 shell no-stub test (registry no longer empty; shell still consumes none of it -- drawer is DR.2). `test:contract` covers the registry; contracts `test/entity.test.ts` proves the DTO-projection typing. Full `scripts/ci.sh` green. |
| DR.2 | INV-CONSOLE-DRAWER-SHELL | LANDED | (this PR) | `@forge/design`: `EntityDrawer` (the drawer body inside the `Drawer` host) + a new `Sparkline` primitive. Data-driven from the DR.1 `EntityDetailView`: each section renders from its `SectionState` -- ok / empty ("none") / not-applicable + unauthorized (ABSENT, not a disabled placeholder) / pending (honest note naming the gating task) / error (inline `role=alert`, degrades that section only) / loading (skeletons). Header = identity + `ScoreRing` + trend `Sparkline` (single-series micro-chart: 2px line, semantic token color, no legend, accessible name carries the values so trend is never color-alone; single point -> honest "no trend yet"). Info/zones/capabilities/policies/recent-decisions rendered with semantic Badges; the quick-action bar renders ONLY the handlers it is given (a PENDING/beyond-tier action is absent, not a dead button). All styling via `--fc-*` tokens (hex-scan gate passes; no new color hex). Fixtures only, no engine coupling. `@forge/design` gains a type-only `@forge/contracts` workspace dep (tsconfig `paths` + vitest alias to source; build resets paths); lockfile updated (workspace link only). 10 new design tests. Full `scripts/ci.sh` green. |
| DR.3 | INV-CONSOLE-DRAWER-BROKERED | LANDED | (DR.3a-d) | The drawer is LIVE end to end. DR.3a synced the wire schema -> @forge/contracts; DR.3b @forge/wire encodes LIST_AGENTS/ENTITY_DECISIONS/ENTITY_CONNECTIONS byte-exact to crdb; DR.3c the CrucibleClient seam; DR.3d-1 OperatorEngine operator-delegated reads; DR.3d-2 `resolveEntityDetail` (tolerant fan-out -> EntityDetailView from LIST_AGENTS + ENTITY_DECISIONS; zones/policies/capabilities honest pending); DR.3d-3 the operator-auth BFF route `GET /api/entity/<kind>/<id>` + the SPA `useEntityDetail` hook + DrawerHost.openEntity rendering the live EntityDrawer. Full path SPA -> BFF -> OperatorEngine -> :7878 -> crdb, gated (console 19, bff 90). |
| DR.4 | INV-CONSOLE-DRAWER-CAPABILITIES (INV-CROSS) | LANDED | (this PR) | `entity.capabilities` now resolves LIVE from the crdb `agent_capabilities` virtual relation (the AIG tools/authority/delegation edges; producer = IP-CONSOLE-CAPABILITIES VR.3, merged). `resolveEntityDetail` fans out a fourth read -- `engine.querySubmit('FIND agent_capabilities WHERE agent_id = $a RETURN relation, target')` -- and projects `(relation, target)` rows into `AgentCapabilityRow` (`USES_TOOL`->tools, `GRANTS_AUTHORITY`->authority, `DELEGATES_TO`->delegation), gated to agents (a non-directory entity is `not-applicable`, a read failure `error`, no edges `empty`; never fabricated). Contract: `CapabilitiesView` made source-honest (`kind:'capabilities'` + `source:'aig-graph'|'construction-report'`), so the drawer UPGRADES to the full signed 10-surface report (Phase B / CR.4) with zero rework -- just a richer `source`. The `EntityDrawer` render already mapped the row shape (DR.2), so no design change. bff 96 tests; full `scripts/ci.sh --skip-net` green. |
| DR.5 | INV-CONSOLE-DRAWER-ACTIONS | IN PROGRESS | (DR.5a) | The quick-action commands (Isolate/Modify VTZ/View Remediation/Open report), audited + confirm-gated (destructive), enforcement OFF for isolate; denial paths tested; `PENDING` where a command is not exposed. **The crdb `Contain` engine op is now LIVE (IP-CONTAIN-COMMAND, crdb main `6cafcfe1`), so `entity.isolate` can be made real.** DR.5a LANDED: synced the vendored wire DTO schema to crdb's committed artifact (adds `WireContain`/`WireContainEffect`/`ContainmentRequest`/`Action`/`AiAssist` + the enriched `WireDecisionRow` technique/evidence/confidence) and regenerated `@forge/contracts` (codegen drift gate green); fixed the ripple in a BFF decision fixture. DR.5b LANDED: the REAL brokered Contain command end to end through the BFF engine layer -- `@forge/wire` dispatch routes `Contain` to the QuerySubmit opcode (a data-plane write the engine discriminates by tag), `wire-client` gains `replyToContainEffect` + a `contain` method (encodes `{Contain}`, decodes `Contained`), the `CrucibleClient` interface gains `contain`, and `OperatorEngine.contain` injects the operator delegation from the authenticated principal (never client-asserted) + records the brokered attempt. Not a stub: it sends the live frame to crdb's `Contain` verb. 1 test (operator delegation injected + recorded + honest effect); the other mocks use the existing `unused` (throw-if-hit) doubles, and the INV-CONSOLE-NO-STUB gate is green. DR.5c LANDED: the BFF Isolate route -- `POST /api/entity/<kind>/<id>/isolate` -> new `resolveIsolate` maps the IsolateRequest to a ContainmentRequest and brokers the live Contain verb (operator delegation injected by the OperatorEngine), mapping the effect back to an IsolateEffect (enforcementActive false, AG.7). Bounded JSON body + strict validation; a denial is a sanitized typed 403 (class only); 401 without a session. 4 tests. Not a stub -- HTTP -> BFF -> OperatorEngine -> crdb Contain. NEXT = DR.5d (the SPA quick-action bar: the Isolate button, confirm-gate, exact effect shown, the other three actions PENDING). |
| DR.6 | INV-CONSOLE-DRAWER-PREFETCH | OPEN | -- | The hover/select prefetch (< 100 ms open) + the unauthorized (tier-absent) state + the stale streaming marker. |
| DR.N | INV-CONSOLE-DRAWER-COMPLETE | OPEN | -- | Playwright E2E: open + real sections + a confirm-gated audited action + tier-gating; all `TRD-CONSOLE-12` Section 7 acceptance rows green. |
