# The Console workspace. Members land with the first implementation PR:
#   packages/*  -- shared libraries (e.g. @forge/contracts, the design system, the query layer)
#   apps/*      -- the deployables (the BFF, the web SPA)
packages:
  - "packages/*"
  - "apps/*"
