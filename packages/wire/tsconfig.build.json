{
  "//": "Build config: emit dist/ from src only; @forge/contracts resolves from its built dist (paths cleared). pnpm -r builds contracts first.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "rootDir": "src",
    "outDir": "dist",
    "paths": {}
  },
  "include": ["src"]
}
