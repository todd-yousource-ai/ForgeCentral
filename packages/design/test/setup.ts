// Vitest setup for @forge/design: register the jest-dom matchers (toBeInTheDocument, toHaveClass,
// toHaveAttribute, ...) and Testing Library's automatic cleanup between tests.
import '@testing-library/jest-dom/vitest';
