{
  "name": "@forge/console",
  "version": "0.0.0",
  "private": true,
  "description": "YouSource Console (ForgeCentral): the operator pane of glass over Crucible, Torch, and Forge.",
  "license": "LicenseRef-YouSource-Proprietary",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22 <23",
    "pnpm": ">=9 <10"
  },
  "scripts": {
    "gate": "bash scripts/ci.sh",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "eslint \"packages/**/*.{ts,tsx}\" \"apps/**/*.{ts,tsx}\" --no-error-on-unmatched-pattern --max-warnings 0",
    "test": "pnpm -r --if-present run test",
    "test:contract": "pnpm -r --if-present run test:contract",
    "test:e2e": "pnpm -r --if-present run test:e2e",
    "build": "pnpm -r --if-present run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:licenses": "node scripts/check-licenses.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "prettier": "^3.4.2",
    "eslint": "^9.17.0",
    "typescript-eslint": "^8.19.0"
  },
  "pnpm": {
    "//": "Force patched transitive dev deps so `pnpm audit --audit-level=high` is clean. vitest 3.2's default resolution keeps an older vite (<=6.4.2) which carries the server.fs.deny bypass (GHSA-fx2h-pf6j-xcff) and pulls a vulnerable esbuild; vite >=6.4.3 depends on esbuild >=0.25. Dev-only (never in dist/). Revisit when vitest bumps its bundled vite.",
    "overrides": {
      "vite": "^6.4.3",
      "esbuild": "^0.25.0"
    }
  }
}
